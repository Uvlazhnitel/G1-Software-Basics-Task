{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build hello world",
            "type": "shell",
            "command": "clang++",
            "args": [
                "-std=c++17", // Use C++17 standard
                "-stdlib=libc++", // Use the LLVM C++ standard library
                "${file}", // Compile the current open file
                "-o", // Specify the output file
                "${fileDirname}/${fileBasenameNoExtension}" // Output filename without extension
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Task generated by Compiler."
        }
    ]
}

 